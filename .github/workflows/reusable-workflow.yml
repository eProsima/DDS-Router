# Reusable workflow to run the following jobs:
#
#   - multiplatform-tests
#     - [ Debug | Release ]
#     - execute tests in different versions of ubuntu with different build types
#

name: reusable-workflow

on:

  workflow_call:
    inputs:

      custom_version_build:
        description: >
          Version of Fast DDS build from eProsima-CI.
        required: true
        type: string

      dependencies_artifact_postfix:
        description: >
          Postfix name to add to artifact name to download dependencies.
          This is use to download a specific artifact version from eProsima-CI.
        required: true
        default: '_nightly'
        type: string

      ref:
        description: >
          The branch or tag name to checkout.
        required: true
        type: string
        default: 'main'

      os:
        description: 'Specify runs-on machine to download specific artifact'
        required: true
        type: string

env:
  code_packages_names: 'ddsrouter_core ddsrouter_yaml ddsrouter_tool'
  docs_packages_names: 'ddsrouter_docs'
  # Number of times ctest runs each test, all of which must pass. Raised above 1 to catch flaky
  # tests: a single green run says little for a test that fails intermittently.
  TEST_ITERATIONS: '10'

jobs:

#####################################################################
# TEST

  multiplatform-tests:
    name: ${{ inputs.os }}-ci-${{ matrix.cmake_build_type }}
    runs-on: ${{ inputs.os }}
    strategy:
      fail-fast: false
      matrix:
        cmake_build_type:
          - Release
          - Debug

    steps:

      - name: Sync repository
        uses: eProsima/eProsima-CI/external/checkout@v0
        with:
          path: src
          ref: ${{ inputs.ref }}

      - name: Download dependencies and install requirements
        uses: ./src/.github/actions/project_dependencies
        with:
          os: ${{ inputs.os }}
          cmake_build_type: ${{ matrix.cmake_build_type }}
          custom_version_build: ${{ inputs.custom_version_build }}
          dependencies_artifact_postfix: ${{ inputs.dependencies_artifact_postfix }}
          secret_token: ${{ secrets.GITHUB_TOKEN }}

      # NOTE: build and test are separate steps, rather than the combined colcon_build_test action,
      # so that the tests can be repeated without paying for a rebuild each time. ctest repeats every
      # test up to TEST_ITERATIONS times and requires all of the runs to pass, which is what surfaces
      # flaky tests instead of hiding them; --repeat until-pass would do the opposite.
      - name: Compile
        uses: eProsima/eProsima-CI/multiplatform/colcon_build@v0
        with:
          colcon_build_args: '--packages-up-to ${{ env.code_packages_names }}'
          cmake_args: -DBUILD_TESTS=ON -DCMAKE_BUILD_TYPE=${{ matrix.cmake_build_type }}
          cmake_build_type: ${{ matrix.cmake_build_type }}
          workspace_dependencies: install
          colcon_meta_file: src/.github/workflows/configurations/${{ runner.os }}/colcon.meta

      - name: Run tests
        id: compile_and_test
        uses: eProsima/eProsima-CI/multiplatform/colcon_test@v0
        with:
          packages_names: ${{ env.code_packages_names }}
          workspace_dependencies: install
          ctest_args: --label-exclude "xfail" --repeat until-fail:${{ env.TEST_ITERATIONS }}
          colcon_meta_file: src/.github/workflows/configurations/${{ runner.os }}/colcon.meta
          test_report_artifact: test_report${{ inputs.dependencies_artifact_postfix }}_${{ inputs.custom_version_build }}_${{ inputs.os }}_${{ matrix.cmake_build_type }}


      - name: Test Report
        uses: eProsima/eProsima-CI/external/test-reporter@v0
        if: success() || failure()
        with:
          name: "Report: ${{ inputs.os }} | ${{ matrix.cmake_build_type }} "
          path: "${{ steps.compile_and_test.outputs.ctest_results_path }}*.xml"
          working-directory: 'src'
          path-replace-backslashes: 'true'
          list-tests: 'failed'
          list-suites: 'failed'
