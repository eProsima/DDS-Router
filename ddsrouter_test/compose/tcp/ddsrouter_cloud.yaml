version: v3.0

participants:

  - name: Echo_Participant
    kind: echo
    data: true

  - name: Cloud_Participant_1
    kind: wan
    listening-addresses:
      - domain: ddsrouter_cloud
        port: 11666
        transport: tcp

  - name: Cloud_Participant_2
    kind: wan
    listening-addresses:
      - domain: ddsrouter_cloud
        port: 21666
        transport: tcp
