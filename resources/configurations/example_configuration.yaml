
#################################
# Topics Allowed

allowlist:
  [
    {name: "HelloWorldTopic", type: "HelloWorld"}     # Allow HelloWorldTopic with type HelloWorld
  ]

local_participant:
  type: "simple"
  domain: 11

#################################
# Simple Participant in domain 0

SimpleParticipant:
  type: "local"
  domain: 0

#################################
# k8s WAN connection Participant

k8s:
  type: "wan"
  id: 2
  connection-addresses:
    [
      id: 0
      {
        ip: "8.8.8.8",
        port: 26000,
        transport: "tcp"
      }
    ]

#################################
# Local Discovery Server using ROS2 default Guid Prefix

local:
  type: local-discovery-server
  ros-discovery-server: true
  listening-addresses:
  [
    {
      ip: "127.0.0.1",
      port: 11666
    }
  ]

#################################
