
# This .yaml contains the configuration for the trivial test.
# It is composed by two dummy participants.

allowlist:
  [
    {name: "trivial_topic", type: "trivial_type"}
  ]

participant_1:
  type: "dummy"

participant_2:
  type: "dummy"
